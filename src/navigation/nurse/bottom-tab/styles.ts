import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const nurseBottomTabStyles = StyleSheet.create({
  profileImage: {
    width: wp(24),
    height: wp(24),
    borderRadius: wp(24) / 2,
  },
});
