import {StyleSheet} from 'react-native';

export const formHeaderStyles = () =>
  StyleSheet.create({
    container: {
      width: '80%',
      alignContent: 'center',
      gap: 8,
      marginBottom: 32,
    },
  });
