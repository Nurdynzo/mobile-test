import {StyleSheet} from 'react-native';

export const authHeaderStyles = () =>
  StyleSheet.create({
    container: {
      width: '80%',
      justifyContent: 'center',
      alignContent: 'center',
      alignItems: 'center',
      gap: 16,
      alignSelf: 'center',
      marginBottom: 32,
      marginTop: 76,
    },
  });
