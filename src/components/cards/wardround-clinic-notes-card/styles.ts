import {StyleSheet} from 'react-native';

export const wardroundClinicNotesStyles = StyleSheet.create({
  container: {
    gap: 12,
    backgroundColor: 'white',
    padding: 12,
    borderRadius: 10,
  },
});
