import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const presentingComplaintsTypeNotesStyles = () => {
  return StyleSheet.create({
    saveButton: {marginTop: wp(32), marginBottom: 124},
  });
};
